{
  "name": "authentication",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/services/authentication/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/services/authentication",
        "main": "apps/services/authentication/src/main.ts",
        "tsConfig": "apps/services/authentication/tsconfig.app.json",
        "webpackConfig": "apps/services/authentication/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "authentication:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "authentication:build:development"
        },
        "production": {
          "buildTarget": "authentication:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/services/authentication/jest.config.ts"
      }
    }
  },
  "tags": []
}
